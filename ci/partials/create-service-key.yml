platform: linux
inputs: [name: src]
outputs:
  - name: s3-credentials
params:
    CF_API: ((cf-api))
    CF_ORG: ((cf-org))
    CF_SPACE: ((cf-space))
    BUCKET_NAME:
    APP_ENV:
run:
  path: src/ci/tasks/create-service-key.sh