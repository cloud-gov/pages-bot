{
  "name": "pages-bot",
  "version": "0.1.6",
  "description": "A bot to handle admin tasks related to the Pages platform.",
  "main": "index.js",
  "scripts": {
    "sync-airtable": "node --env-file=.env ./tasks/sync-airtable.js",
    "test": "node --test",
    "verify-db-queries": "node --env-file=.env ./tasks/verify-db-queries.js"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "^24.x.x",
    "npm": "^11.10.0"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "knex": "^3.1.0",
    "pg": "^8.12.0"
  }
}
